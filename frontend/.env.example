# Copy this file to ".env" and fill in your values.
# Used by the React frontend (Vite reads VITE_* at build time).

# Deployed SmartAge Payments contract id (from scripts/deploy.sh output).
VITE_CONTRACT_ADDRESS=

# Network: "futurenet" (default) or "testnet".
VITE_NETWORK=futurenet
