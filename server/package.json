{
  "name": "smart-age-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SmartAge unified server — frontend serving + REST API + contract indexer",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "typecheck": "node --check src/index.js && node --check src/api.js && node --check src/indexer.js && node --check src/db.js && node --check src/stellar.js && node --check src/config.js"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^13.1.0",
    "better-sqlite3": "^11.5.0",
    "express": "^4.21.1"
  }
}
